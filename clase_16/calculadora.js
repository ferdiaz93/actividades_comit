let tareas = [
    { description: 'pasear al perro', done: false },
]

tareas = [];


function sumar(num1, num2) {
    return (num1 + num2);
}

function restar(num1, num2) {
    return (num1 - num2);
}

module.exports = { sumar, restar }